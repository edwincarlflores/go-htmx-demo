package main

import (
	"github.com/edwincarlflores/go-htmx/app"
)

func main() {
	app.AppTempl()
	// app.AppHtml() // Comment out this one if you want to run the server that uses plain .html files as templates
}
